Matched stratified random-split comparator for IEEE-CIS.
Uses the same feature set, preprocessing, models, training size, calibration and metrics as the verified temporal benchmark.
Full mode: 4 independent stratified replicates; 300k train, 40k validation, 40k calibration, 50k test; logistic, LightGBM, XGBoost; raw and Platt.
